# Tipo de Documento Fiscal Configurable + Mejora UI/UX — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir configurar globalmente el tipo de documento fiscal (RFC/NIF/Cédula/Otro con etiqueta personalizada) con labels dinámicos en toda la UI, y mejorar el diseño con botones elegantes, tokens de diseño y responsive correcto.

**Architecture:** Campo interno `rfc` de las entidades no cambia (retrocompatible con hojas existentes). Dos claves nuevas en `Config` (`tipo_doc`, `tipo_doc_etiqueta`) + helper puro `getDocLabel()`. UI: tema compartido `theme.css` con `@theme` de Tailwind 4 importado por las dos apps, overhaul de `Button`/`Input`/`Select`/`Card`/`Dialog`, y grids responsivas.

**Tech Stack:** React 19 · TypeScript 5 (strict) · Tailwind CSS 4 · Vitest 3 · WXT 0.20 · Vite 6 · Zod 4

## Global Constraints

- TypeScript `strict: true`. Sin `any` en tipos públicos.
- El campo de documento de las entidades sigue llamándose `rfc` internamente. **No** se toca el esquema de hojas.
- `getDocLabel(tipo, etiqueta)`: `RFC`→`'RFC'`, `NIF`→`'NIF'`, `Cedula`→`'Cédula'`, `Otro`→`etiqueta.trim() || 'Documento fiscal'`.
- Defaults de Config: `tipo_doc: 'RFC'`, `tipo_doc_etiqueta: ''`.
- Hojas existentes sin las claves nuevas caen a los defaults vía `configFromRows`. Sin migración.
- El campo documento (`rfc`/`empresa_rfc`) sigue opcional. Sin validación extra.
- Todos los cambios de UI mantienen la API actual de `Button` (`variant`, `className`) — no se rompen usos existentes.
- Tests corren con `pnpm test` (vitest, workspace shared). Los feature tests ya stub `fetch` global.
- Commits convencionales (`feat:`, `fix:`, `chore:`, `style:`).

---

### Task 1: Helper `taxid.ts` + tests (TDD)

**Files:**
- Create: `packages/shared/src/taxid.ts`
- Test: `packages/shared/tests/taxid.test.ts`

**Interfaces:**
- Produces: `type TipoDoc = 'RFC' | 'NIF' | 'Cedula' | 'Otro'`; `const TIPO_DOC_OPTIONS: { value: TipoDoc; label: string }[]`; `function getDocLabel(tipo: TipoDoc, etiqueta: string): string`

- [ ] **Step 1: Write the failing test**

Create `packages/shared/tests/taxid.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { getDocLabel, TIPO_DOC_OPTIONS } from '../src/taxid'

describe('taxid', () => {
  it('TIPO_DOC_OPTIONS expone los 4 tipos', () => {
    expect(TIPO_DOC_OPTIONS.map(o => o.value)).toEqual(['RFC', 'NIF', 'Cedula', 'Otro'])
    expect(TIPO_DOC_OPTIONS.find(o => o.value === 'Cedula')?.label).toBe('Cédula')
  })
  it('getDocLabel devuelve el label del tipo fijo', () => {
    expect(getDocLabel('RFC', '')).toBe('RFC')
    expect(getDocLabel('NIF', '')).toBe('NIF')
    expect(getDocLabel('Cedula', '')).toBe('Cédula')
  })
  it('Otro usa la etiqueta personalizada', () => {
    expect(getDocLabel('Otro', 'RUT')).toBe('RUT')
    expect(getDocLabel('Otro', '  DNI  ')).toBe('DNI')
  })
  it('Otro sin etiqueta cae a Documento fiscal', () => {
    expect(getDocLabel('Otro', '')).toBe('Documento fiscal')
    expect(getDocLabel('Otro', '   ')).toBe('Documento fiscal')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm -F shared test -- tests/taxid.test.ts`
Expected: FAIL — `Cannot find module '../src/taxid'`

- [ ] **Step 3: Write minimal implementation**

Create `packages/shared/src/taxid.ts`:

```ts
export type TipoDoc = 'RFC' | 'NIF' | 'Cedula' | 'Otro'

export const TIPO_DOC_OPTIONS: { value: TipoDoc; label: string }[] = [
  { value: 'RFC', label: 'RFC' },
  { value: 'NIF', label: 'NIF' },
  { value: 'Cedula', label: 'Cédula' },
  { value: 'Otro', label: 'Otro' }
]

export function getDocLabel(tipo: TipoDoc, etiqueta: string): string {
  switch (tipo) {
    case 'RFC': return 'RFC'
    case 'NIF': return 'NIF'
    case 'Cedula': return 'Cédula'
    case 'Otro': return etiqueta.trim() || 'Documento fiscal'
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm -F shared test -- tests/taxid.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add packages/shared/src/taxid.ts packages/shared/tests/taxid.test.ts
git commit -m "feat: helper tipo documento fiscal (RFC/NIF/Cédula/Otro)"
```

---

### Task 2: Modelo Config — claves `tipo_doc` y `tipo_doc_etiqueta`

**Files:**
- Modify: `packages/shared/src/types/entities.ts:82-95` (interfaz `Config`)
- Modify: `packages/shared/src/types/schemas.ts:71-84` (`ConfigSchema`)
- Modify: `packages/shared/src/sheets/createSpreadsheet.ts:9-22,45-67` (`DEFAULT_CONFIG`, `configFromRows`)
- Test: `packages/shared/tests/taxid.test.ts`

**Interfaces:**
- Consumes: `TipoDoc` de `../taxid` (Task 1)
- Produces: `Config` con `tipo_doc: TipoDoc` y `tipo_doc_etiqueta: string`

- [ ] **Step 1: Write the failing test**

Append to `packages/shared/tests/taxid.test.ts`:

```ts
import { ConfigSchema } from '../src/types/schemas'
import { configFromRows } from '../src/sheets/createSpreadsheet'

describe('config tipo_doc', () => {
  it('ConfigSchema default tipo_doc RFC', () => {
    const c = ConfigSchema.parse({ empresa_nombre: 'X', prefijo_folio: 'FAC-' })
    expect(c.tipo_doc).toBe('RFC')
    expect(c.tipo_doc_etiqueta).toBe('')
  })
  it('configFromRows lee tipo_doc de la hoja', () => {
    const c = configFromRows([['tipo_doc', 'NIF'], ['tipo_doc_etiqueta', '']])
    expect(c.tipo_doc).toBe('NIF')
  })
  it('configFromRows sin claves cae a default RFC', () => {
    const c = configFromRows([['empresa_nombre', 'A']])
    expect(c.tipo_doc).toBe('RFC')
    expect(c.tipo_doc_etiqueta).toBe('')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm -F shared test -- tests/taxid.test.ts`
Expected: FAIL — `c.tipo_doc` is `undefined`

- [ ] **Step 3: Implement model changes**

`packages/shared/src/types/entities.ts` — añadir al final de la interfaz `Config` (después de `categorias_cxp`):

```ts
  tipo_doc: TipoDoc
  tipo_doc_etiqueta: string
```

Añadir import al inicio del archivo:

```ts
import type { TipoDoc } from '../taxid'
```

`packages/shared/src/types/schemas.ts` — añadir al objeto `ConfigSchema` (después de `categorias_cxp`):

```ts
  tipo_doc: z.enum(['RFC', 'NIF', 'Cedula', 'Otro']).default('RFC'),
  tipo_doc_etiqueta: z.string().default('')
```

`packages/shared/src/sheets/createSpreadsheet.ts` — en `DEFAULT_CONFIG` (después de `categorias_cxp`):

```ts
  tipo_doc: 'RFC' as const,
  tipo_doc_etiqueta: ''
```

En `configFromRows` (después de la línea de `categorias_cxp`):

```ts
    tipo_doc: (map.get('tipo_doc') as Config['tipo_doc']) || DEFAULT_CONFIG.tipo_doc,
    tipo_doc_etiqueta: map.get('tipo_doc_etiqueta') ?? DEFAULT_CONFIG.tipo_doc_etiqueta
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm -F shared test -- tests/taxid.test.ts`
Expected: PASS

- [ ] **Step 5: Run full suite**

Run: `pnpm test`
Expected: todos los tests verdes (38 + 3 nuevos + 4 de Task 1)

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/types/entities.ts packages/shared/src/types/schemas.ts packages/shared/src/sheets/createSpreadsheet.ts packages/shared/tests/taxid.test.ts
git commit -m "feat: claves tipo_doc y tipo_doc_etiqueta en Config"
```

---

### Task 3: UI de Configuración — select tipo_doc + etiqueta + label dinámico

**Files:**
- Modify: `packages/shared/src/features/configuracion/Configuracion.tsx`
- Test: `packages/shared/tests/configuracion.test.tsx`

**Interfaces:**
- Consumes: `TIPO_DOC_OPTIONS`, `getDocLabel` (Task 1); `Config['tipo_doc']` (Task 2)

- [ ] **Step 1: Write the failing test**

Reemplazar el contenido de `packages/shared/tests/configuracion.test.tsx`:

```ts
import { describe, expect, it, beforeAll, afterAll, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import React from 'react'
import { Configuracion } from '../src/features/configuracion/Configuracion'
import { AppProvider } from '../src/store/queries'
import { createRepository } from '../src/data/repository'
import { SheetsApi } from '../src/sheets/api'

beforeAll(() => {
  vi.stubGlobal('fetch', vi.fn(async () => ({ ok: true, json: async () => ({ valueRanges: [] }) })))
})
afterAll(() => vi.unstubAllGlobals())

const wrapper = ({ children }: { children: React.ReactNode }) => {
  const repo = createRepository({ api: new SheetsApi(async () => 'T'), storage: { get: async () => null, set: async () => {}, remove: async () => {} }, getSpreadsheetId: async () => 'S' })
  return <AppProvider repo={repo}>{children}</AppProvider>
}

describe('Configuracion', () => {
  it('renderiza sección de empresa', async () => {
    render(<Configuracion />, { wrapper })
    expect(await screen.findByText(/datos de la empresa/i)).toBeTruthy()
  })
  it('muestra el select de tipo de documento', async () => {
    render(<Configuracion />, { wrapper })
    expect(await screen.findByText(/tipo de documento/i)).toBeTruthy()
    expect(screen.getAllByText('RFC').length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm -F shared test -- tests/configuracion.test.tsx`
Expected: FAIL — no encuentra "Tipo de documento"

- [ ] **Step 3: Implement**

En `packages/shared/src/features/configuracion/Configuracion.tsx`:

1. Añadir imports:

```ts
import { TIPO_DOC_OPTIONS, getDocLabel } from '../../taxid'
```

2. Antes del `return`, derivar el label:

```ts
  const docLabel = getDocLabel(form.tipo_doc, form.tipo_doc_etiqueta)
```

3. Reemplazar el `Card title="Datos de la empresa"` completo (líneas 32-43) por:

```tsx
      <Card title="Datos de la empresa">
        <div className="space-y-3">
          <div><label className="text-xs text-gray-500">Nombre *</label><Input value={form.empresa_nombre} onChange={set('empresa_nombre')} /></div>
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
            <div>
              <label className="text-xs text-gray-500">Tipo de documento</label>
              <Select value={form.tipo_doc} onChange={v => setForm(f => f && ({ ...f, tipo_doc: v as Config['tipo_doc'] }))} options={TIPO_DOC_OPTIONS} />
            </div>
            <div>
              <label className="text-xs text-gray-500">{docLabel}</label>
              <Input value={form.empresa_rfc} onChange={set('empresa_rfc')} />
            </div>
            {form.tipo_doc === 'Otro' && (
              <div className="sm:col-span-2">
                <label className="text-xs text-gray-500">Etiqueta personalizada del documento</label>
                <Input value={form.tipo_doc_etiqueta} onChange={set('tipo_doc_etiqueta')} placeholder="Ej. RUT, DNI, CUIT…" />
              </div>
            )}
            <div><label className="text-xs text-gray-500">Teléfono</label><Input value={form.empresa_telefono} onChange={set('empresa_telefono')} /></div>
          </div>
          <div><label className="text-xs text-gray-500">Email</label><Input value={form.empresa_email} onChange={set('empresa_email')} /></div>
          <div><label className="text-xs text-gray-500">Dirección</label><Input value={form.empresa_direccion} onChange={set('empresa_direccion')} /></div>
          <div><label className="text-xs text-gray-500">Logo (URL)</label><Input value={form.empresa_logo} onChange={set('empresa_logo')} /></div>
        </div>
      </Card>
```

4. En el `Card title="Facturación"`, cambiar el grid (línea 45) de `grid grid-cols-2 gap-3` a `grid grid-cols-1 sm:grid-cols-2 gap-3`.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm -F shared test -- tests/configuracion.test.tsx`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add packages/shared/src/features/configuracion/Configuracion.tsx packages/shared/tests/configuracion.test.tsx
git commit -m "feat: select tipo de documento y etiqueta en Configuración"
```

---

### Task 4: Labels dinámicos en cliente/proveedor

**Files:**
- Modify: `packages/shared/src/features/clientes/ClienteFormModal.tsx:21`
- Modify: `packages/shared/src/features/clientes/Clientes.tsx:16,23,31`
- Modify: `packages/shared/src/features/proveedores/ProveedorFormModal.tsx:17`
- Modify: `packages/shared/src/features/proveedores/Proveedores.tsx:24`

**Interfaces:**
- Consumes: `getDocLabel`, `TipoDoc` (Task 1); `useConfig` de `../../store/queries` (existe)

- [ ] **Step 1: Implement (cambio mecánico, verificado por suite existente + compilación)**

`packages/shared/src/features/clientes/ClienteFormModal.tsx`:

1. Añadir imports:

```ts
import { useConfig } from '../../store/queries'
import { getDocLabel } from '../../taxid'
```

2. Dentro del componente (tras `const set = ...`):

```ts
  const { config } = useConfig()
  const docLabel = getDocLabel(config?.tipo_doc ?? 'RFC', config?.tipo_doc_etiqueta ?? '')
```

3. Línea 21, label `RFC` → `{docLabel}`:

```tsx
        <div><label className="text-xs text-gray-500">{docLabel}</label><Input value={form.rfc} onChange={set('rfc')} /></div>
```

`packages/shared/src/features/clientes/Clientes.tsx`:

1. Añadir imports:

```ts
import { useConfig } from '../../store/queries'
import { getDocLabel } from '../../taxid'
```

2. Tras `const [deleteId, setDeleteId] = ...`:

```ts
  const { config } = useConfig()
  const docLabel = getDocLabel(config?.tipo_doc ?? 'RFC', config?.tipo_doc_etiqueta ?? '')
```

3. Línea 23 placeholder → `` placeholder={`Buscar nombre o ${docLabel}…`} ``
4. Línea 31 header `'RFC'` → `docLabel`

`packages/shared/src/features/proveedores/ProveedorFormModal.tsx`: mismo patrón que ClienteFormModal (imports `useConfig`, `getDocLabel`; línea 17 label → `{docLabel}`).

`packages/shared/src/features/proveedores/Proveedores.tsx`: mismo patrón que Clientes (imports; línea 24 header `'RFC'` → `docLabel`).

- [ ] **Step 2: Run typecheck**

Run: `pnpm exec tsc --noEmit -p packages/shared`
Expected: sin errores

- [ ] **Step 3: Run full suite**

Run: `pnpm test`
Expected: verdes (incluye `clientes.test.tsx` que ahora dispara `useConfig` — fetch ya está stubeado)

- [ ] **Step 4: Commit**

```bash
git add packages/shared/src/features/clientes packages/shared/src/features/proveedores
git commit -m "feat: labels dinámicos de documento fiscal en clientes y proveedores"
```

---

### Task 5: Design tokens — `theme.css` + import en apps

**Files:**
- Create: `packages/shared/src/ui/theme.css`
- Modify: `apps/web/src/index.css`
- Modify: `apps/extension/entrypoints/dashboard/dashboard.css`

**Interfaces:**
- Produces: tokens Tailwind 4 — colores `primary`, `primary-hover`, `primary-soft`, `danger`; sombras `shadow-card`, `shadow-card-hover`, `shadow-btn`

- [ ] **Step 1: Create `packages/shared/src/ui/theme.css`**

```css
@theme {
  --color-primary: oklch(0.546 0.245 262.881);
  --color-primary-hover: oklch(0.488 0.243 264.376);
  --color-primary-soft: oklch(0.97 0.014 254.604);
  --color-danger: oklch(0.577 0.245 27.325);
  --shadow-card: 0 1px 3px rgb(0 0 0 / 0.08);
  --shadow-card-hover: 0 4px 12px rgb(0 0 0 / 0.1);
  --shadow-btn: 0 1px 2px rgb(0 0 0 / 0.1);
}
```

- [ ] **Step 2: Import en ambas apps**

`apps/web/src/index.css`:

```css
@import 'tailwindcss';
@import '../../../packages/shared/src/ui/theme.css';
@source '../../../packages/shared/src';
```

`apps/extension/entrypoints/dashboard/dashboard.css`:

```css
@import 'tailwindcss';
@import '../../../../packages/shared/src/ui/theme.css';
@source '../../../../packages/shared/src';
```

- [ ] **Step 3: Verify compilación de ambas apps**

Run: `pnpm -F web build && pnpm -F extension build`
Expected: ambos builds OK (confirma que los tokens se resuelven)

- [ ] **Step 4: Commit**

```bash
git add packages/shared/src/ui/theme.css apps/web/src/index.css apps/extension/entrypoints/dashboard/dashboard.css
git commit -m "style: design tokens compartidos (theme.css) en web y extensión"
```

---

### Task 6: Overhaul del componente `Button`

**Files:**
- Modify: `packages/shared/src/ui/components.tsx:6-14`
- Test: `packages/shared/tests/ui.test.tsx`

**Interfaces:**
- Produces: `Button` con `variant: 'primary' | 'outline' | 'danger' | 'ghost' | 'success'`, `size: 'sm' | 'md' | 'lg'`, `icon?: ReactNode`, `iconAfter?: ReactNode`, `iconOnly?: boolean`. API anterior (`variant`, `className`, children) intacta.

- [ ] **Step 1: Write the failing test**

Append a `packages/shared/tests/ui.test.tsx`:

```ts
import { render, screen } from '@testing-library/react'
import { Button } from '../src/ui/components'

describe('Button', () => {
  it('renderiza con variante success', () => {
    render(<Button variant="success">Guardar</Button>)
    const btn = screen.getByText('Guardar')
    expect(btn.className).toContain('bg-green-600')
  })
  it('size lg agrega padding grande', () => {
    render(<Button size="lg">Grande</Button>)
    expect(screen.getByText('Grande').className).toContain('px-5')
  })
  it('iconOnly no renderiza texto', () => {
    const { container } = render(<Button iconOnly aria-label="editar">✎</Button>)
    expect(container.textContent).toBe('')
  })
  it('icon se renderiza antes del texto', () => {
    render(<Button icon={<span data-testid="ico">+</span>}>Crear</Button>)
    expect(screen.getByTestId('ico')).toBeTruthy()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm -F shared test -- tests/ui.test.tsx`
Expected: FAIL — `bg-green-600` no está en className (variante success inexistente)

- [ ] **Step 3: Implement**

En `packages/shared/src/ui/components.tsx`, reemplazar el componente `Button` (líneas 6-14) por:

```tsx
export type ButtonVariant = 'primary' | 'outline' | 'danger' | 'ghost' | 'success'
export type ButtonSize = 'sm' | 'md' | 'lg'

export function Button({ variant = 'primary', size = 'md', icon, iconAfter, iconOnly, className, children, ...props }: React.ButtonHTMLAttributes<HTMLButtonElement> & { variant?: ButtonVariant; size?: ButtonSize; icon?: ReactNode; iconAfter?: ReactNode; iconOnly?: boolean }) {
  const styles: Record<ButtonVariant, string> = {
    primary: 'bg-primary text-white shadow-btn hover:bg-primary-hover',
    outline: 'border border-gray-300 bg-white text-gray-700 hover:bg-gray-50',
    danger: 'bg-danger text-white hover:opacity-90',
    ghost: 'text-gray-700 hover:bg-gray-100',
    success: 'bg-green-600 text-white hover:bg-green-700'
  }
  const sizes: Record<ButtonSize, string> = {
    sm: iconOnly ? 'p-2' : 'px-2.5 py-1.5 text-xs',
    md: iconOnly ? 'p-2.5' : 'px-3.5 py-2 text-sm',
    lg: iconOnly ? 'p-3' : 'px-5 py-2.5 text-base'
  }
  return (
    <button
      {...props}
      className={cx(
        'inline-flex items-center justify-center gap-2 rounded-lg font-medium transition-all duration-150',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/50 focus-visible:ring-offset-1',
        'active:scale-[0.98] disabled:opacity-50 disabled:pointer-events-none',
        styles[variant], sizes[size], className
      )}
    >
      {icon && <span className="inline-flex">{icon}</span>}
      {!iconOnly && children}
      {iconAfter && <span className="inline-flex">{iconAfter}</span>}
    </button>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm -F shared test -- tests/ui.test.tsx`
Expected: PASS

- [ ] **Step 5: Run full suite**

Run: `pnpm test`
Expected: verdes (usos existentes de `Button` siguen funcionando)

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/ui/components.tsx packages/shared/tests/ui.test.tsx
git commit -m "style: overhaul Button con variantes, tamaños, iconos y focus ring"
```

---

### Task 7: Input/Select/Card/StatCard/Badge/Table/Dialog polish

**Files:**
- Modify: `packages/shared/src/ui/components.tsx` (Input, Select, Card, StatCard, Badge, Table, Dialog)

**Interfaces:**
- Consumes: tokens de Task 5 (`primary`, `danger`, `shadow-card`, `shadow-card-hover`)
- Produces: `Input`/`Select` con focus ring y altura consistente; `Dialog` bottom-sheet en móvil

- [ ] **Step 1: Implement**

En `packages/shared/src/ui/components.tsx`:

**Input** (líneas 16-18) → reemplazar:

```tsx
export function Input(props: React.InputHTMLAttributes<HTMLInputElement>) {
  return <input {...props} className={cx('w-full h-9 sm:h-10 px-3.5 border border-gray-300 rounded-lg text-sm transition-colors focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/30', props.className)} />
}
```

**Select** (líneas 20-27) → reemplazar el `className` del `<select>`:

```tsx
    <select value={value} onChange={e => onChange(e.target.value)} className="w-full h-9 sm:h-10 px-3.5 border border-gray-300 rounded-lg text-sm transition-colors focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/30">
```

**Card** (línea 31) → `bg-white border border-gray-200 rounded-xl shadow-card`

**StatCard** (línea 42) → `bg-white border border-gray-200 rounded-xl p-4 shadow-card hover:shadow-card-hover transition-shadow`

**Badge** (líneas 49-59) → solo cambia el tono `blue`:

```ts
    blue: 'bg-primary-soft text-primary'
```

**Table** (líneas 62-77) → añadir contenedor redondeado y mantener header:

```tsx
  return (
    <div className="overflow-x-auto rounded-xl border border-gray-200">
      <table className="min-w-full text-sm">
        <thead className="bg-gray-50 text-left text-xs uppercase text-gray-500">
          <tr>{columns.map(c => <th key={c.key} className="px-4 py-2 whitespace-nowrap">{c.header}</th>)}</tr>
        </thead>
        <tbody>
          {rows.map((r, i) => (
            <tr key={i} className="border-t border-gray-100 hover:bg-gray-50">
              {columns.map(c => <td key={c.key} className="px-4 py-2 whitespace-nowrap">{c.render ? c.render(r) : String(r[c.key] ?? '')}</td>)}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
```

**Dialog** (líneas 83-84) → reemplazar el contenedor y panel:

```tsx
    <div className="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-black/40 p-0 sm:p-4" onClick={onClose}>
      <div className="bg-white w-full sm:max-w-lg max-h-[90vh] overflow-auto rounded-t-2xl sm:rounded-xl" onClick={e => e.stopPropagation()}>
```

**Sweep de wrappers** — cambiar `rounded-lg` → `rounded-xl shadow-card` en los wrappers de tabla/cards de features (mecánico, 8 sitios):

- `packages/shared/src/features/clientes/Clientes.tsx:27`
- `packages/shared/src/features/proveedores/Proveedores.tsx:21`
- `packages/shared/src/features/dashboard/Dashboard.tsx:39,48`
- `packages/shared/src/features/facturas/Facturas.tsx` (wrapper tabla)
- `packages/shared/src/features/gastos/Gastos.tsx` (wrapper tabla)
- `packages/shared/src/features/cuentasPagar/CuentasPagar.tsx` (wrapper tabla)
- `packages/shared/src/features/reportes/Reportes.tsx` (tarjetas)

- [ ] **Step 2: Run typecheck + suite**

Run: `pnpm test`
Expected: verdes (ui.test.tsx, feature tests)

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/ui/components.tsx packages/shared/src/features
git commit -m "style: inputs, cards, tablas y dialog con tokens y responsive"
```

---

### Task 8: Responsive — dashboard KPIs, formularios gasto/cxp, Layout tokens

**Files:**
- Modify: `packages/shared/src/features/dashboard/Dashboard.tsx:28`
- Modify: `packages/shared/src/features/gastos/GastoFormModal.tsx:30,37`
- Modify: `packages/shared/src/features/cuentasPagar/CxpFormModal.tsx:31,38`
- Modify: `packages/shared/src/ui/layout/Layout.tsx:24`

**Interfaces:**
- Consumes: tokens `primary`, `primary-soft` (Task 5)

- [ ] **Step 1: Implement**

`packages/shared/src/features/dashboard/Dashboard.tsx:28` — grid de KPIs:

```tsx
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
```

`packages/shared/src/features/gastos/GastoFormModal.tsx:30,37` — `grid grid-cols-2 gap-3` → `grid grid-cols-1 sm:grid-cols-2 gap-3` (ambas apariciones).

`packages/shared/src/features/cuentasPagar/CxpFormModal.tsx:31,38` — idem.

`packages/shared/src/ui/layout/Layout.tsx:24` — item activo de la nav:

```tsx
          className={cx('w-full flex items-center gap-3 px-4 py-3 text-sm', current === n.key ? 'bg-primary-soft text-primary font-medium border-l-4 border-primary' : 'text-gray-700 hover:bg-gray-100')}>
```

- [ ] **Step 2: Run typecheck + suite**

Run: `pnpm test`
Expected: verdes

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/features/dashboard/Dashboard.tsx packages/shared/src/features/gastos/GastoFormModal.tsx packages/shared/src/features/cuentasPagar/CxpFormModal.tsx packages/shared/src/ui/layout/Layout.tsx
git commit -m "style: responsive en dashboard, formularios y nav con tokens"
```

---

### Task 9: Verificación final + docs

**Files:**
- Modify: `FUNCIONALIDADES.md` (tabla de Config, sección 3.1)
- Modify: `docs/superpowers/specs/2026-08-13-taxid-ui-design.md` (estado → implementado, si aplica)

- [ ] **Step 1: Actualizar docs**

En `FUNCIONALIDADES.md`, sección `**Config**` (tabla de claves), añadir tras `iva_porcentaje`:

```md
| `tipo_doc` | string | `RFC` | (RFC / NIF / Cédula / Otro) |
| `tipo_doc_etiqueta` | string | `''` | Etiqueta personalizada si `tipo_doc = Otro` |
```

- [ ] **Step 2: Verificación completa**

Run: `pnpm test`
Expected: verde (45 tests: 38 previos + 7 nuevos)

Run: `pnpm -F web build && pnpm -F extension build`
Expected: ambos builds OK

- [ ] **Step 3: Verificación manual (vista móvil <640px y desktop)**

1. Configuración: select "Tipo de documento" cambia la etiqueta del campo emisor; al elegir "Otro" aparece "Etiqueta personalizada".
2. Cliente/Proveedor: label del documento refleja la config; formularios y tablas sin desbordes en móvil.
3. Dashboard: 2 columnas de KPIs en móvil, 4 en desktop.
4. Dialog de edición: bottom-sheet en móvil, centrado en desktop.
5. Botones: hover/focus/active/disabled se ven correctos.

- [ ] **Step 4: Commit**

```bash
git add FUNCIONALIDADES.md docs/superpowers/specs/2026-08-13-taxid-ui-design.md
git commit -m "docs: actualiza FUNCIONALIDADES con claves tipo_doc"
```
